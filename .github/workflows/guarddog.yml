name: GuardDog Full Security Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  guarddog:
    name: GuardDog - Full Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install GuardDog
        run: pip install guarddog

      - name: 🧪 Scan dependencies (package.json)
        run: guarddog npm verify ./package.json


      - name: 🐶 Local files scan
        id: local_scan
        continue-on-error: true
        run: |
          guarddog npm scan .


      - name: ❗ Final check for failures
        run: |
          deps_count="${{ steps.deps_scan.outputs.count }}"
          local_count="${{ steps.local_scan.outputs.count }}"
          echo "Dependency issues: $deps_count"
          echo "Local issues: $local_count"

          if [ "$deps_count" -gt 0 ] || [ "$local_count" -gt 0 ]; then
            echo "❌ GuardDog scan failed"
            exit 1
          else
            echo "✅ All GuardDog scans passed"
          fi
